class Movie {
	constructor(readonly id: string, readonly title: string, readonly durationInMinutes: number) {
				this.id = id;
				this.title = title;
				this.durationInMinutes = durationInMinutes;
	}
}


export default Movie